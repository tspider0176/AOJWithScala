import scala.io.StdIn

object Main{
  def main(args: Array[String]){
    val num = StdIn.readLine().split(" ")
    val arr1 = Array[Int]()();

    for(i <- 0 until num(0).toInt){
      val line = StdIn.readLine().split(" ")
      
    }

    for(i <- 0 until in1(0).toInt){
      for(j <- 0 until in1(1).toInt){
        arr1(i)(j) =
      }
    }

  }
}
